# 🍾 ย้ายบ้านข้อมูล P&K (โรงขวด) 2025 → 2026

> ย้ายข้อมูลจาก AppSheet 4 แอปของ **โรงขวด P&K** ในบัญชีเก่า **palm.work2025@gmail.com**
> มาทำใหม่เป็นเว็บแอป + GAS ในบัญชีปัจจุบัน **palm.work2026@gmail.com**
> โหมดที่คุณปาล์มเลือก: **ย้ายครั้งเดียว แล้วปิดของเก่า** (ไม่ทำซิงก์สองทาง)

## ⚠️ P&K เป็นอีกบริษัท — คนละบริษัทกับโรงน้ำ

- ทั้ง 4 แอปนี้เป็นของ**โรงขวด P&K** ไม่ใช่โรงน้ำละกอน/ออริจิ้น แล็บส์
- **ห้าม**จับคู่ข้อมูลเข้า OriginSystem / Lakon Factory / Sale System เด็ดขาด — สร้างสเปรดชีตกลางใหม่ของ P&K แยกต่างหาก
- โค้ดระบบใหม่ของ P&K: อยู่ใน repo นี้ (**`pk-hq`** — แยกออกมาจาก palm-hq เมื่อ 30 ส.ค. ตามแบบ `origin-hq`/`olddays-hq`)
- GAS ฝั่ง P&K ที่มีแล้วในเมลใหม่: **P&K HR** (สร้าง 28 ส.ค.)

## แอปที่ย้าย (ทั้ง 4 แอปในเมลเก่า)

| แอปเก่า (AppSheet) | สถานะ | ปลายทางในระบบใหม่ของ P&K |
|---|---|---|
| **Office** | 🟢 Deployed — ตัวเดียวที่ใช้จริงอยู่ | งานธุรการ/พนักงานโรงขวด → GAS **P&K HR** (มีแล้ว) |
| **งานผลิต** | Prototype | แอปงานผลิต P&K (สร้างใหม่: เว็บแอป + GAS) |
| **รับออเดอร์** | Prototype | แอปรับออเดอร์ P&K (สร้างใหม่) |
| **ทีมสกรีนสี** | Prototype | แอปทีมสกรีนสี P&K (สร้างใหม่ — งานสกรีนสีขวด) |

## ขั้นตอน (5 ขั้น)

| # | ใคร | ทำอะไร |
|---|---|---|
| 1 | คุณปาล์ม (~5 นาที) | เข้าเมลเก่า → Drive → โฟลเดอร์ `appsheet/data` → **Share ทั้งโฟลเดอร์** ให้ `palm.work2026@gmail.com` (Viewer พอ) |
| 2 | คุณปาล์ม | เลือก **วันตัดยอด** แล้วหยุดกรอกข้อมูลในแอปเก่า (สำคัญกับ **Office** ที่ Deployed อยู่) |
| 3 | สคริปต์ | รัน `migrate-data.gs` (ไฟล์ในโฟลเดอร์นี้) ในเมลใหม่ → ก๊อปทุกสเปรดชีต+ไฟล์แนบเข้าโฟลเดอร์ `Migration-2025` + สร้างชีต `Migration_Report` |
| 4 | ทีม AI | รัน `verifyReport()` เทียบแท็บ×แถวทุกไฟล์ → ออกแบบสเปรดชีตกลาง P&K + สร้างเว็บแอปใหม่ (โค้ดในโฟลเดอร์ `pk/` ของ repo นี้) |
| 5 | คุณปาล์ม | ยืนยันข้อมูลครบ → เลิกใช้/ลบแอป AppSheet เก่า — ต้นฉบับในเมลเก่ายังอยู่เป็น backup |

## กติกาความปลอดภัย

- สคริปต์ **อ่านอย่างเดียว** กับของเก่า: ทำสำเนา (เจ้าของ = เมลใหม่) ไม่แก้/ลบ/ย้ายต้นฉบับ
- โฟลเดอร์ใหญ่ไม่ติดลิมิต 6 นาที: สคริปต์พักเองแล้วตั้ง trigger รันต่ออัตโนมัติ
- ตรวจรับด้วย `Migration_Report` — คอลัมน์ "ตรวจ" ต้องเป็น `OK` ทุกแถวก่อนปิดของเก่า
- **ข้อมูล P&K แยกจากชีตโรงน้ำ/คาเฟ่เด็ดขาด** — คนละบริษัท คนละสเปรดชีต

## สถานะ (อัปเดต 30 ส.ค. 2026)

- [x] ขั้น 1 — แชร์แล้ว (โฟลเดอร์ `appsheet` + ชีต "ระบบบัญชี")
- [x] ขั้น 3 (บางส่วน) — สแนปช็อต "ระบบบัญชี" → `PK_ระบบบัญชี_snapshot_2026-08-30` ในเมลใหม่แล้ว
- [x] สร้างระบบใหม่ v1-v2 แล้ว → root ของ repo นี้ (แอปทีมงาน + บอร์ดบริหาร + GAS) — **รอคุณปาล์มวาง GAS ครั้งแรก** ตาม `README.md`
- [ ] ขั้น 2 — ยังไม่เคาะวันตัดยอด (คุณปาล์มบอก "เสร็จเรียบร้อยค่อยเปลี่ยน")
- [ ] ชีตของแอป งานผลิต / ทีมสกรีน / รับออเดอร์ / Office_iPad ยังไม่ถูกแชร์ — โฟลเดอร์ appsheet/data มีแต่ empty.txt (ชีตจริงอยู่ที่อื่นใน Drive เมลเก่า) → เช็คใน AppSheet → Data → Source แล้วแชร์เพิ่มถ้าอยากเก็บประวัติงานเก่า

### ผลเช็คระบบเก่า (audit 30 ส.ค.)

- ไฟล์ "ระบบบัญชี" (สร้าง ก.ค. 2024): บิลรายวัน 2024–2025 + รายการเครดิต (ลูกหนี้) + ทะเบียนลูกค้า ~578 ราย
- **บิลเก่าเก็บแค่ยอดรวม ไม่มีรายการสินค้า/จำนวน/ราคาต่อหน่วย** → ระบบใหม่เพิ่ม line-items = ฐานของฟีเจอร์ออกบิล
- วันที่ปนหลายรูปแบบ (2/04, 02/04/2025, 5/06) · ชื่อลูกค้าซ้ำ/สะกดหลายแบบ · มีแถวว่างแทรก · แท็บเครดิตกับแท็บบิลรายวันอาจบันทึกบิลเดียวกันซ้ำ
- ยืนยันตามที่คุณปาล์มพูด: 4 แอปเดิม**ไม่ซิงก์กัน** — บัญชีแยกจากออเดอร์/ผลิต/สกรีนโดยสิ้นเชิง

แผนฉบับอ่านง่าย (หน้าเว็บ): ดูลิงก์ artifact ในแชทที่สร้างงานนี้
