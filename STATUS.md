# STATUS — สถานะระบบจริง ณ ตอนนี้ (เขียนทับไฟล์นี้เสมอ **ห้าม append ต่อท้าย**)

> ไฟล์นี้ตอบคำถามเดียว: **"โค้ดใน repo กับของที่รันจริง ตรงกันรึยัง / อะไรรอวาง"**
> อัปเดตล่าสุด: 2026-08-30 โดยแชทเลขา palm-hq — แยก repo · แก้ import ชนเพดาน 6 นาที · **วาง GAS + ต่อ URL + เปิด Pages ครบแล้ว ระบบพร้อมใช้**

## ของจริงที่รันอยู่ เทียบกับ repo

| ชิ้นส่วน | ที่รันจริง | สถานะ |
|---|---|---|
| หน้าเว็บทั้งหมด (`index.html` / `exec/` / `finance/`) | GitHub Pages ของ repo นี้ — `https://palmsil2026.github.io/pk-hq/` | ✅ **เปิดแล้ว 30 ส.ค.** (build run #1 สำเร็จ) — push `main` = ขึ้นจริงใน ~1 นาที |
| GAS **P&K System** (`gas/Code.gs` เวอร์ชัน `2026-08-30d`) | deployment `AKfycbw8guY7h7Q_BCfl_RwlD6PQn5fmhXmsu1myjr60OGwbEiAwB_PVE59iAsZmgY9meWH-` | ✅ **ตรงกับ repo** — ยิง `?action=pkHealth` ตอบ `version 2026-08-30d` + `sheet: true` · ทดสอบแล้ว: ล็อกอินผิดตอบ error ถูกต้อง · เรียก action ที่ต้องล็อกอินโดยไม่มี token ถูกบล็อก · **URL นี้ต่อเข้า `index.html` + `exec/index.html` แล้ว** (แก้โค้ดครั้งหน้า: Manage deployments → ✏️ → New version เท่านั้น + bump `CODE_VERSION`) |
| GAS **Bottle Finance** (`finance/gas/Code.gs` เวอร์ชัน `bottle-fin 2026-08-29a`) | — | ⏳ ยังไม่สร้างโปรเจกต์ — ตาม `finance/README.md` · ⚠️ **อ่าน "ประเด็นที่ต้องเคาะ" ข้างล่างก่อนติดตั้ง** |
| GAS **P&K HR** (เงินเดือน — โค้ดอยู่ repo `origin-hq` → `payroll-app/` v2.3) | deployment `AKfycbwosxTyYyw8oE_xBC3jhZjk3ZOe7WpEpXDxbYr0Qmo8UoA-Khb_bPTiVsBqr4dYxWF4` @9 | ✅ **วางแล้ว** (smoke test ผ่าน 2026-08-30) — เหลือคุณปาล์มกด **🔧 ติดตั้งระบบ** ครั้งแรกในแอป (ชีต `P&KSystem` ฝั่ง HR ยังว่าง) + เพิ่มพนักงานโรงขวด |

## ⏳ รอคุณปาล์ม (เรียงตามลำดับที่ควรทำ)

1. ~~เปิด GitHub Pages~~ ✅ **เสร็จแล้ว** — แอปทีมงาน `https://palmsil2026.github.io/pk-hq/` · บอร์ดบริหาร `…/pk-hq/exec/?key=รหัส`
2. ~~วาง GAS + deploy + ต่อ URL เข้าแอป~~ ✅ **เสร็จแล้ว 30 ส.ค.** — เหลือ 2 อย่างในโปรเจกต์นี้:
   · **รัน `importLegacyAccounting()` ให้จบ** (รุ่น `d` เขียนรวดเดียว จบในไม่กี่วินาที) — ถ้ารอบก่อนค้างไว้ รันซ้ำได้เลย ไม่ต้องล้างชีต ระบบข้ามของที่เข้าไปแล้วเอง · เช็คผลจาก Execution log บรรทัด "นำเข้าเสร็จ: บิล … ลูกหนี้เครดิต … ลูกค้าใหม่ …"
   · **เพิ่มพนักงาน (ชื่อเล่น+PIN)** ในบอร์ดบริหาร → จัดการพนักงาน → แล้วให้ทีมล็อกอินหน้าแอป
3. **P&K HR**: เปิดแอปเงินเดือน (ลิงก์ deployment ตารางบน) → กด **🔧 ติดตั้งระบบ** (ระบบ bootstrap ชีตเปล่า จะโชว์ผู้ใช้ "ผู้จัดการโรงงาน" + **PIN สุ่ม 6 หลักครั้งเดียว — จดทันที**) → นำเข้าไฟล์สแกนนิ้ว `.xlsx` งวดแรก (แท็บ เงินสด/เงินโอน — ระบบจะจำ "รหัสสแกน→ชื่อ") → เพิ่มทะเบียนพนักงานโรงขวด **~33 คน** (เงินสด 11 + เงินโอน 22) จากรายชื่อที่แอปเจอ + เช็คเคสค้าง "อ้ายวุน/บุญ" ที่อยู่แท็บต้นฉบับแต่ไม่อยู่แท็บย่อย
4. **เคาะประเด็นระบบบิล** (ข้างล่าง) — แล้วค่อยตัดสินใจว่าจะติดตั้ง Bottle Finance หรือไม่
5. **แผนย้ายข้อมูลเก่า** (`docs/migration-2025/`): ยังค้าง ① แชร์ชีตของแอป งานผลิต / ทีมสกรีน / รับออเดอร์ / Office_iPad จากเมลเก่า ถ้าอยากเก็บประวัติงานพวกนั้น (ตอนนี้ได้มาแค่ "ระบบบัญชี") ② เคาะ**วันตัดยอด**หยุดกรอกแอปเก่า ③ ตรวจ `Migration_Report` ครบแล้วค่อยเลิกใช้ AppSheet เก่า
6. เก่าใน palm-hq: branch `claude/migrate-gas-sync-data-y4ghqt` / `claude/organize-bottling-products-xyrtew` / `claude/brewery-finance-app-catr9s` ถูกย้ายเนื้อหามา repo นี้ครบแล้ว — **ไม่ต้อง merge** ลบทิ้งได้เลย

## ⚠️ ประเด็นที่ต้องเคาะ: ระบบบิลมี 2 ทางซ้อนกัน

สองแชททำคู่ขนานกัน (29–30 ส.ค.) ได้ระบบบิลคนละแนว — **ยังไม่ได้เลือกทางหลัก**:

| | `finance/` (Bottle Finance) | P&K System (`gas/Code.gs`) |
|---|---|---|
| แนวคิด | ตัวช่วยเร็ว: จดบิลลงแท็บใหม่ `App_Bills` ใน**ชีทเช็คบิลเดิม**ของฝ่ายการเงิน ไม่ย้ายข้อมูล | ระบบครบวงจร: บิลผูกออเดอร์/สต๊อก อยู่แท็บ `Bills`+`Statements` ในชีต `PKSystem` + import ข้อมูลเก่า |
| ข้อดี | ฝ่ายการเงินใช้ได้ทันที ไม่กระทบใคร | บิลเชื่อมผลิต-ส่งของ-ลูกหนี้ในที่เดียว ตรวจย้อนได้ |
| ความเสี่ยงถ้าใช้คู่กัน | **บิลถูกจดสองที่ ยอดลูกหนี้สองเวอร์ชัน** — ขัดหลัก "จุดซิงก์เดียว" | — |

ข้อเสนอทีม AI: ถ้าจะลงระบบใหม่ทั้งชุดอยู่แล้ว → ใช้ **P&K System เป็นทางหลัก** (ข้อ 2 ข้างบน) แล้วเก็บ `finance/` ไว้เป็นตัวเลือก "หน้าจอเฉพาะฝ่ายการเงิน" ที่ค่อยดัดให้อ่าน/เขียน `PKSystem.Bills` ทีหลัง · ถ้าอยากให้ฝ่ายการเงินมีของใช้เลยระหว่างรอ → ติดตั้ง `finance/` ก่อนได้ แต่พอ P&K System ขึ้นจริง **ต้องเลิกจดที่หนึ่งในสอง** ทันที — คุณปาล์มเคาะ

## 🗃️ สเปรดชีต 3 ไฟล์ของ P&K (อย่าสับสน)

| ชีต | ใครสร้าง/ใช้ | สถานะ |
|---|---|---|
| `PKSystem` | `gas/Code.gs` สร้างเองตอนรัน `setupPkSystem()` — ฐานระบบกลาง | ✅ สร้างแล้ว (`pkHealth` ยืนยัน `sheet: true`) · ข้อมูลเก่าอาจเข้าไม่ครบถ้า import รอบแรกค้าง — รันซ้ำให้จบ |
| `P&KSystem` | แอปเงินเดือน P&K HR (โค้ดใน origin-hq) | มีไฟล์แล้วแต่**ยังว่าง** — รอกด 🔧 ติดตั้งระบบ |
| ชีทเช็คบิลฝ่ายการเงิน (ของเดิม) | ฝ่ายการเงินใช้อยู่จริงทุกวัน · `finance/` จะเพิ่มแท็บ `App_Bills` | ใช้งานอยู่ (id อยู่ใน `finance/README.md`) |

> สแนปช็อตข้อมูลเก่า: `PK_ระบบบัญชี_snapshot_2026-08-30` (id ฝังใน `gas/Code.gs` ตัวแปร `LEGACY_SNAPSHOT_ID`)

## วิธีดูแลไฟล์นี้

แก้ `.gs` แล้วยังไม่วาง GAS → จดในตาราง "ของจริงที่รันอยู่" · คุณปาล์มวาง+New version เสร็จ → อัปเดตสถานะเป็น ✅ · หน้าเว็บไม่ต้องจด (push `main` = ขึ้นเอง)
