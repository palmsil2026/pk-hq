# STATUS — สถานะระบบจริง ณ ตอนนี้ (เขียนทับไฟล์นี้เสมอ **ห้าม append ต่อท้าย**)

> ไฟล์นี้ตอบคำถามเดียว: **"โค้ดใน repo กับของที่รันจริง ตรงกันรึยัง / อะไรรอวาง"**
> อัปเดตล่าสุด: 2026-08-30 โดยแชทเลขา palm-hq
> **รอบล่าสุด `2026-08-30k`: ตั้งระบบ deploy หลังบ้านอัตโนมัติ (clasp) + โชว์เลขเวอร์ชันในแอป + โครงชีตปรับตามให้เอง**
> — เลิกก๊อปวาง GAS มือ เหมือน repo โรงน้ำ · **เหลือคุณปาล์มทำ 2 อย่างครั้งเดียว: ใส่ Secret `CLASPRC_JSON` + บอก Script ID** (ดู `DEPLOY.md`)
> ก่อนหน้า (หน้าเว็บอย่างเดียว): มุมมอง **"รวมตามสินค้า"** ในแท็บออเดอร์ — ต้นไม้ ประเภท → ขนาด → สี → ใบสั่ง พร้อมยอดรวมทุกชั้น เหมือนหน้า "ออเดอร์" ของแอปเดิม
> ก่อนหน้า `2026-08-30j` (**GAS ยังรอวางทับ**): ปรับตามหน้าจอแอปเดิมที่คุณปาล์มส่งรูปมา (ปุ่มแทน dropdown · แบรนด์ · พรีฟอร์ม · ทีมสกรีนแตะทีเดียวจบ) · `i`: แตะออเดอร์ดูรายละเอียด + แท็บประวัติ · `g`+`h`: เกรด B · เครื่องจักร · ขั้นต่ำสั่ง · ลูกค้า 225 ราย · seed/import ปรับโครงชีตเอง

## ของจริงที่รันอยู่ เทียบกับ repo

| ชิ้นส่วน | ที่รันจริง | สถานะ |
|---|---|---|
| หน้าเว็บทั้งหมด (`index.html` / `exec/` / `finance/`) | GitHub Pages ของ repo นี้ — `https://palmsil2026.github.io/pk-hq/` | ✅ **เปิดแล้ว 30 ส.ค.** (build run #1 สำเร็จ) — push `main` = ขึ้นจริงใน ~1 นาที |
| GAS **P&K System** (repo = `2026-08-30k`) | deployment `AKfycbw8guY7h7Q_BCfl_RwlD6PQn5fmhXmsu1myjr60OGwbEiAwB_PVE59iAsZmgY9meWH-` รัน `2026-08-30d` | ⏳ **รอวางทับด้วย `k`** (ตัดลงเวลา + ฟอร์มสเปก + เกรด B/เครื่องจักร + ขั้นต่ำ + import ออเดอร์/ลูกค้า) แล้ว Manage deployments → ✏️ → New version · ของเดิม `d` — ยิง `?action=pkHealth` ตอบ `version 2026-08-30d` + `sheet: true` · ทดสอบแล้ว: ล็อกอินผิดตอบ error ถูกต้อง · เรียก action ที่ต้องล็อกอินโดยไม่มี token ถูกบล็อก · **URL นี้ต่อเข้า `index.html` + `exec/index.html` แล้ว** (แก้โค้ดครั้งหน้า: Manage deployments → ✏️ → New version เท่านั้น + bump `CODE_VERSION`) |
| GAS **Bottle Finance** (`finance/gas/Code.gs` เวอร์ชัน `bottle-fin 2026-08-29a`) | — | ⏳ ยังไม่สร้างโปรเจกต์ — ตาม `finance/README.md` · ⚠️ **อ่าน "ประเด็นที่ต้องเคาะ" ข้างล่างก่อนติดตั้ง** |
| GAS **P&K HR** (เงินเดือน — โค้ดอยู่ repo `origin-hq` → `payroll-app/` v2.3) | deployment `AKfycbwosxTyYyw8oE_xBC3jhZjk3ZOe7WpEpXDxbYr0Qmo8UoA-Khb_bPTiVsBqr4dYxWF4` @9 | ✅ **วางแล้ว** (smoke test ผ่าน 2026-08-30) — เหลือคุณปาล์มกด **🔧 ติดตั้งระบบ** ครั้งแรกในแอป (ชีต `P&KSystem` ฝั่ง HR ยังว่าง) + เพิ่มพนักงานโรงขวด |

## ⏳ รอคุณปาล์ม (เรียงตามลำดับที่ควรทำ)

1. ~~เปิด GitHub Pages~~ ✅ **เสร็จแล้ว** — แอปทีมงาน `https://palmsil2026.github.io/pk-hq/` · บอร์ดบริหาร `…/pk-hq/exec/?key=รหัส`
2. ~~วาง GAS + deploy + ต่อ URL เข้าแอป~~ ✅ **เสร็จแล้ว 30 ส.ค.** — เหลือ 2 อย่างในโปรเจกต์นี้:
   · **รัน `importLegacyAccounting()` ให้จบ** (รุ่น `d` เขียนรวดเดียว จบในไม่กี่วินาที) — ถ้ารอบก่อนค้างไว้ รันซ้ำได้เลย ไม่ต้องล้างชีต ระบบข้ามของที่เข้าไปแล้วเอง · เช็คผลจาก Execution log บรรทัด "นำเข้าเสร็จ: บิล … ลูกหนี้เครดิต … ลูกค้าใหม่ …"
   · **เพิ่มพนักงาน (ชื่อเล่น+PIN)** ในบอร์ดบริหาร → จัดการพนักงาน → แล้วให้ทีมล็อกอินหน้าแอป
3. **P&K HR**: เปิดแอปเงินเดือน (ลิงก์ deployment ตารางบน) → กด **🔧 ติดตั้งระบบ** (ระบบ bootstrap ชีตเปล่า จะโชว์ผู้ใช้ "ผู้จัดการโรงงาน" + **PIN สุ่ม 6 หลักครั้งเดียว — จดทันที**) → นำเข้าไฟล์สแกนนิ้ว `.xlsx` งวดแรก (แท็บ เงินสด/เงินโอน — ระบบจะจำ "รหัสสแกน→ชื่อ") → เพิ่มทะเบียนพนักงานโรงขวด **~33 คน** (เงินสด 11 + เงินโอน 22) จากรายชื่อที่แอปเจอ + เช็คเคสค้าง "อ้ายวุน/บุญ" ที่อยู่แท็บต้นฉบับแต่ไม่อยู่แท็บย่อย
4. **เคาะประเด็นระบบบิล** (ข้างล่าง) — แล้วค่อยตัดสินใจว่าจะติดตั้ง Bottle Finance หรือไม่
5. **แผนย้ายข้อมูลเก่า** (`docs/migration-2025/`): ยังค้าง ① แชร์ชีตของแอป งานผลิต / ทีมสกรีน / รับออเดอร์ / Office_iPad จากเมลเก่า ถ้าอยากเก็บประวัติงานพวกนั้น (ตอนนี้ได้มาแค่ "ระบบบัญชี") ② เคาะ**วันตัดยอด**หยุดกรอกแอปเก่า ③ ตรวจ `Migration_Report` ครบแล้วค่อยเลิกใช้ AppSheet เก่า
6. เก่าใน palm-hq: branch `claude/migrate-gas-sync-data-y4ghqt` / `claude/organize-bottling-products-xyrtew` / `claude/brewery-finance-app-catr9s` ถูกย้ายเนื้อหามา repo นี้ครบแล้ว — **ไม่ต้อง merge** ลบทิ้งได้เลย

## 📚 สิ่งที่ถอดได้จากออเดอร์จริง 136 ใบ (ชีต "Order" ของ AppSheet — คุณปาล์มแชร์มา 30 ส.ค.)

ระบบใหม่ถูกปรับให้ตรงกับของจริงแล้ว ตามนี้:

**1. สินค้าไม่ใช่ชื่อเดียว — เป็นสเปก 5 แกน** `ประเภท × ขนาด × คอขวด × รูปทรง × สี` (+ สีสกรีน + พร้อมฝาสี)
ประเภท: ขวด(114) ถัง(28) ฟิล์มหุ้มคอ(14) ฝา(10) ฟิล์มแพ็คโหล(4) ถุงหิ้ว(2) ลัง(2) · คลังคำศัพท์ทั้งหมดอยู่ที่ `const SPEC` ใน `gas/Code.gs`

**2. นับเป็น "ถุง" ไม่ใช่ใบ** — ถอดตัวคูณจากข้อมูลจริงได้สะอาด ไม่มีขัดแย้งเลย (`const PER_BAG`):
ขวด 250=252 · 350=300 · 500=250 · 600=200 · 800=190 · 1500=100 · น้ำลัง(920)=130 · ถัง 18.9 ลิตร=5 (ใบ/ถุง)
ฟอร์มใหม่ใส่จำนวนถุง แล้วคูณเป็นใบให้เอง + บวกเศษใบได้

**3. เรื่องที่แอปเก่าไม่มีช่อง พนักงานเขียนปนใน "หมายเหตุ" (43% ของออเดอร์)** — ตอนนี้มีช่องจริงแล้ว:
| เดิมเขียนมือ | ช่องใหม่ |
|---|---|
| "รอโอน" · "รอโอนเงิน" · "ทำได้เลย" | `สถานะเงิน` |
| "รอแบบ" · "แก้ไขแบบ" · "ทำแบบ" | `สถานะแบบ` |
| "มีเก็บค่าบล้อค 500 บาท" | `ค่าบล็อก` |
| "ขอด่วน" · "ไปภายในอาทิตนี้" | `ด่วน` |
| "เสร็จ3/9/68" (35 ออเดอร์!) | `วันเสร็จจริง` |
| "ทำ25ถุงค้าง9ถุง(ขวดหมด)" | `ทำแล้ว(ถุง)` · `ค้าง(ถุง)` · `สาเหตุค้าง` |

`parseLegacyNote_()` แกะได้ **47/59 หมายเหตุ** — ที่เหลือเป็นข้อความอิสระจริง (นัดรับของ/หน่วยพิเศษ) เก็บไว้ในหมายเหตุตามเดิม ถูกต้องแล้ว

**4. ระบบเก่าไม่ได้คิดเงิน** — `ราคาต่อหน่วย` กรอกแค่ 18% · `ราคาทั้งหมด` 0% (เงินอยู่ชีต "ระบบบัญชี" แยก) · ระบบใหม่ใส่ราคาได้แต่ไม่บังคับ

### ✅ ทำตามของเดิมแล้ว (รอบ `g`)
- **เกรด B** — ของเดิมแยกผล 3 ทาง (ผลิตได้/เกรด B/ของเสีย) ระบบใหม่มีครบแล้ว · เกรด B **ไม่บวกเข้าสต๊อกของดี** (กันยอดขายได้เกินจริง) — ถ้าจะขายเกรด B ต้องเคาะว่าตั้งเป็นสินค้าแยกหรือลดราคา
- **ทะเบียนเครื่องฉีด 12 ตัว** (`seedMachines()`) — ลงผลผลิตเลือกเครื่องจาก dropdown แล้ว · เพิ่ม/ปิดเครื่องแก้ในแท็บ `Machines` ไม่ต้องแก้โค้ด
- **ขั้นต่ำสั่ง** — เตือนตอนลงออเดอร์ (250ml=2,000 · 600ml=1,200 · ถัง=25) **เตือนอย่างเดียวไม่บล็อก** เพราะหน้างานยืดหยุ่นได้
- **ทะเบียนลูกค้า 225 ราย + 16 เส้นทางส่ง** (`importLegacyCustomers()`) — ช่องเลือกลูกค้าโชว์ "สาย 119 · งาว" กันเลือกผิดคนชื่อคล้ายกัน

### ⚠️ ยังขาด — ต้องการจากคุณปาล์ม
1. ~~ชีตแอป "ทีมสกรีน"~~ ✅ **เห็นหน้าจอจริงแล้ว** — และพบว่า**ทีมสกรีนไม่ได้กรอกจำนวนเลย** แค่แตะทีม → แตะเสร็จ · ระบบใหม่ปรับตามแล้ว
2. ~~ชีตแอป "Office"~~ ✅ **เห็นหน้าจอจริงแล้ว** (คุณปาล์มส่งรูป 30 ส.ค.) — ถอดลงใน `docs/legacy-apps/README.md` หัวข้อ "หน้าจอจริงของแอปเดิม"
   · ⚠️ **ข้อมูลสดใหม่กว่าชีตที่ผมอ่าน** — หน้าจอโชว์ออเดอร์ปี 2026 และคอลัมน์ `แบรนด์` ถูกใช้จริง แต่ชีตที่แชร์มาเป็นข้อมูลปี 2568 ที่ `แบรนด์` ว่าง 100% → ถ้าจะ import ให้ครบ ต้องขอชีตตัวที่แอปใช้อยู่ตอนนี้
3. **ราคาจริง** — ตารางราคาในชีต Stock Management มีคอลัมน์ `ราคาหน้าร้าน`/`ราคาโรงงาน`/`ราคาไม่รวมฝา` แต่**ว่างทั้งหมด**
4. _(มีก็ดี)_ รูปหน้าจอแอปเดิม 3-4 รูป · เกรด B ขายยังไง · เส้นทางส่งวิ่งวันไหน

### 🔁 โครงชีตปรับตามเวอร์ชันให้เองแล้ว (ตั้งแต่ `2026-08-30k`)
เดิมวางโค้ดรุ่นใหม่แล้วต้องรัน `setupPkSystem()` เองทุกครั้ง (ลืมแล้วขึ้น "ไม่พบแท็บ Machines" — เจอจริง 30 ส.ค.)
ตอนนี้ `autoMigrate_()` ทำให้เองที่คำขอแรกหลัง `CODE_VERSION` เปลี่ยน แล้วจำไว้ใน Script Property `SCHEMA_AT` (ครั้งเดียวต่อเวอร์ชัน)
เติมคอลัมน์ต่อท้ายอย่างเดียว ไม่แทรกกลาง ไม่ลบ ไม่แตะข้อมูลเก่า · เช็คได้จาก `?action=pkHealth` → ช่อง `schemaAt`
**ยกเว้นครั้งแรกสุดตอนยังไม่มีชีต** ต้องรัน `setupPkSystem()` ใน editor เอง 1 ครั้ง · ส่วน `seed*`/`importLegacy*` เป็นการนำเข้าข้อมูล ยังต้องสั่งเอง

### 🚀 deploy หลังบ้านอัตโนมัติ — ตั้งไว้แล้ว รอเปิดสวิตช์
`.github/workflows/deploy-gas.yml` + `.github/scripts/deploy-gas.sh` (ชุดเดียวกับ `origin-hq` ที่ใช้มาตั้งแต่ 24 ส.ค.)
push `.gs` เข้า `main` → clasp push + **redeploy URL เดิม** + smoke test ยิง `pkHealth` จริงว่าได้เวอร์ชันใหม่
② Script ID ของ P&K System = ✅ ใส่แล้ว · **รอคุณปาล์มอย่างเดียว: ① Secret `CLASPRC_JSON`** — วิธีทำอยู่ `DEPLOY.md`
จนกว่าจะเปิด ยังต้องวางมือตามเดิม (Manage deployments → ✏️ → New version)

### 🔢 เลขเวอร์ชันโชว์ในแอปแล้ว
ท้ายหน้าแอปทีมงาน/บอร์ดบริหารมีบรรทัด `เวอร์ชัน · หน้าเว็บ … · ระบบหลังบ้าน …`
สองเลขตรงกัน = ขึ้นครบ · เลขหลังบ้านสีส้ม + แถบเตือนบนหัวแอป = หน้าเว็บใหม่แล้วแต่ `.gs` ยังไม่ได้วาง
(`WEB_VERSION` หัวไฟล์ html · `CODE_VERSION` หัวไฟล์ .gs — แก้ไฟล์ไหน bump ตัวนั้น)

### ✅ ทำตามหน้าจอแอปเดิมครบแล้ว: มุมมอง "รวมตามสินค้า"
แท็บ **ออเดอร์** มีปุ่มสลับ 2 มุมมอง — **รายใบ** (ของเดิม) กับ **รวมตามสินค้า** (ตามหน้า "ออเดอร์" ของแอป Office เดิม)
ต้นไม้ `ประเภท` → `ขนาด` → `สี` → `ใบสั่งของใคร` · ทุกชั้นมียอดรวมเป็นใบ · ชั้นขนาดแปลงเป็นถุงให้ด้วย · แตะใบสั่ง = เปิดรายละเอียดออเดอร์เต็ม
นับเฉพาะออเดอร์ที่ยังไม่จบ (ตัด "ส่งแล้ว"/"ยกเลิก") = ยอด "ของที่ยังต้องทำ" ไม่ใช่ยอดขายย้อนหลัง
**แก้เฉพาะ `index.html` — GAS ไม่เปลี่ยน ไม่ต้อง deploy ใหม่** (อ่านจากข้อมูลก้อนเดิมที่ `pkTeamData` ส่งมาอยู่แล้ว)

### 📌 ยังไม่ได้ทำ (จดไว้เฟสหน้า)
**รอบเครื่องแบบจับเวลา** — ของเดิมมี เวลาเริ่มเครื่อง/เวลาปิด/เวลาทำงานสุทธิ ต่อรอบ (ใช้ดูประสิทธิภาพเครื่อง)
ระบบใหม่ตอนนี้บันทึก "เครื่องไหนผลิตอะไรได้เท่าไหร่" แล้ว แต่ยังไม่จับเวลาเดินเครื่อง — เป็นงานก้อนใหญ่ ทำรอบหน้า

## ⚠️ ประเด็นที่ต้องเคาะ: ระบบบิลมี 2 ทางซ้อนกัน

สองแชททำคู่ขนานกัน (29–30 ส.ค.) ได้ระบบบิลคนละแนว — **ยังไม่ได้เลือกทางหลัก**:

| | `finance/` (Bottle Finance) | P&K System (`gas/Code.gs`) |
|---|---|---|
| แนวคิด | ตัวช่วยเร็ว: จดบิลลงแท็บใหม่ `App_Bills` ใน**ชีทเช็คบิลเดิม**ของฝ่ายการเงิน ไม่ย้ายข้อมูล | ระบบครบวงจร: บิลผูกออเดอร์/สต๊อก อยู่แท็บ `Bills`+`Statements` ในชีต `PKSystem` + import ข้อมูลเก่า |
| ข้อดี | ฝ่ายการเงินใช้ได้ทันที ไม่กระทบใคร | บิลเชื่อมผลิต-ส่งของ-ลูกหนี้ในที่เดียว ตรวจย้อนได้ |
| ความเสี่ยงถ้าใช้คู่กัน | **บิลถูกจดสองที่ ยอดลูกหนี้สองเวอร์ชัน** — ขัดหลัก "จุดซิงก์เดียว" | — |

ข้อเสนอทีม AI: ถ้าจะลงระบบใหม่ทั้งชุดอยู่แล้ว → ใช้ **P&K System เป็นทางหลัก** (ข้อ 2 ข้างบน) แล้วเก็บ `finance/` ไว้เป็นตัวเลือก "หน้าจอเฉพาะฝ่ายการเงิน" ที่ค่อยดัดให้อ่าน/เขียน `PKSystem.Bills` ทีหลัง · ถ้าอยากให้ฝ่ายการเงินมีของใช้เลยระหว่างรอ → ติดตั้ง `finance/` ก่อนได้ แต่พอ P&K System ขึ้นจริง **ต้องเลิกจดที่หนึ่งในสอง** ทันที — คุณปาล์มเคาะ

## 🗃️ สเปรดชีต 3 ไฟล์ของ P&K (อย่าสับสน)

| ชีต | ใครสร้าง/ใช้ | สถานะ |
|---|---|---|
| `PKSystem` | `gas/Code.gs` สร้างเองตอนรัน `setupPkSystem()` — ฐานระบบกลาง | ✅ สร้างแล้ว (`pkHealth` ยืนยัน `sheet: true`) · ข้อมูลเก่าอาจเข้าไม่ครบถ้า import รอบแรกค้าง — รันซ้ำให้จบ |
| `P&KSystem` | แอปเงินเดือน P&K HR (โค้ดใน origin-hq) | มีไฟล์แล้วแต่**ยังว่าง** — รอกด 🔧 ติดตั้งระบบ |
| ชีทเช็คบิลฝ่ายการเงิน (ของเดิม) | ฝ่ายการเงินใช้อยู่จริงทุกวัน · `finance/` จะเพิ่มแท็บ `App_Bills` | ใช้งานอยู่ (id อยู่ใน `finance/README.md`) |

> สแนปช็อตข้อมูลเก่า: `PK_ระบบบัญชี_snapshot_2026-08-30` (id ฝังใน `gas/Code.gs` ตัวแปร `LEGACY_SNAPSHOT_ID`)

## วิธีดูแลไฟล์นี้

แก้ `.gs` แล้วยังไม่วาง GAS → จดในตาราง "ของจริงที่รันอยู่" · คุณปาล์มวาง+New version เสร็จ → อัปเดตสถานะเป็น ✅ · หน้าเว็บไม่ต้องจด (push `main` = ขึ้นเอง)
