# CLAUDE.md — บริบทธุรกิจโรงขวด P&K

ไฟล์นี้เป็น "สมองกลาง" ของ repo นี้ — AI ทุกแชทอ่านก่อนเริ่มงานเสมอ

---

## 🏭 บริษัทนี้คือใคร

- **บริษัท พี แอนด์ เค พลาสติก อินดัสทรี จำกัด** (P&K Plastic Industry Co., Ltd.) — เรียกสั้น ๆ ว่า **"โรงขวด P&K"**
- โทร. 054-250482, 086-4315402 · ลำปาง
- ผลิต/จำหน่ายบรรจุภัณฑ์พลาสติกให้โรงน้ำดื่มในลำปางและใกล้เคียง: **ขวด PET · ฝา · ฟิล์ม · ถัง/แกลลอน · ถ้วยน้ำ · งานสกรีนสีขวด**
- ลูกค้าในบัญชี ~578 ราย (แบรนด์น้ำดื่ม/น้ำแข็ง) — รวมถึง**โรงน้ำละกอน**ของคุณปาล์มเองด้วย (ซื้อขวด/ฝา/ฟิล์มจากที่นี่)
- วงจรงาน: ลูกค้าสั่งขวด → ผลิต → (สกรีนสีถ้ามี) → ส่งของ (เป็นงวดได้) → เก็บเงิน (สด / เครดิต+ใบวางบิล)
- ⚠️ **คนละบริษัทกับโรงน้ำละกอน (`origin-hq`) และร้านกาแฟ (`olddays-hq`)** — ข้อมูล/ชีต/GAS **ห้ามปนกันเด็ดขาด** ห้ามชี้ระบบนี้ไปที่ OriginSystem หรือ GAS ของโรงน้ำ
- ภาษาหลักในการสื่อสาร: **ภาษาไทย** (ตอบเป็นไทยเสมอ เว้นแต่ถูกขอเป็นภาษาอื่น)

## 🗂️ โครง repo ของคุณปาล์ม (4 repo — เปิดแชทให้ตรงโฟลเดอร์)

| repo | คืออะไร |
|---|---|
| `palm-hq` | ตัวคุณปาล์ม: คุณเลขา + ทีม AI + ห้องผู้บริหาร — อ่านข้อมูลทุกบริษัทแบบ read-only |
| `origin-hq` | โรงน้ำละกอน (ออริจิ้น แล็บส์) — เซลส์/โรงงาน/คลัง/สต๊อก/ทีมผลิต/เงินเดือน |
| `olddays-hq` | ร้านกาแฟ Old Days |
| **`pk-hq` (repo นี้)** | **โรงขวด P&K ทั้งบริษัท** — แยกออกมาจาก palm-hq เมื่อ 2026-08-30 |

งานแตะโค้ดของบริษัทไหน → เปิดแชทในโฟลเดอร์ repo นั้น อย่าแก้ข้ามที่

## 🧩 ระบบ/ฐานข้อมูลของ P&K (3 ก้อน — คนละสเปรดชีต อย่าสับสน)

| # | ระบบ | โค้ดอยู่ที่ | หลังบ้าน (สเปรดชีต) |
|---|---|---|---|
| 1 | **P&K System** — แอปทีมงาน (`index.html`) + บอร์ดบริหาร (`exec/`) + API (`gas/Code.gs`) | repo นี้ | ชีต **`PKSystem`** (GAS สร้างเองด้วย `setupPkSystem()`) — Orders/Production/ScreenJobs/Bills/Statements/Products/Customers/PriceHistory |
| 2 | **แอปลงบัญชี** (`account/`) — ลงบิล/รับชำระเครดิต/เงินเข้าบัญชีไหน สำหรับฝ่ายบัญชี | repo นี้ | **ไม่มี GAS แยก** — ใช้ P&K System (ข้อ 1) + แท็บ `Bills` เดียวกัน · แผนก "บัญชี" · (`finance/` เก่า = พักไว้ ไม่ติดตั้ง) |
| 3 | **เงินเดือน + HR** — GAS โปรเจกต์ **P&K HR** | repo **`origin-hq`** → `payroll-app/` (โค้ดชุดเดียว deploy 2 โปรเจกต์: โรงน้ำ+โรงขวด — **ห้ามก๊อปสำเนามาไว้ที่นี่**) | ชีต **`P&KSystem`** ของฝั่ง HR (คนละไฟล์กับ `PKSystem` ข้อ 1!) |

- ระบบเดิมเป็น **AppSheet 4 แอป** ในเมลเก่า `palm.work2025@gmail.com` (Office / งานผลิต / รับออเดอร์ / ทีมสกรีนสี) — กำลัง**ย้ายครั้งเดียวแล้วปิดของเก่า** → แผน+สคริปต์อยู่ `docs/migration-2025/`
- ทะเบียนสินค้ามาตรฐาน (SKU 6 หมวด, 29 รายการ): `docs/products/`
- อะไรวางของจริงแล้ว/ยังรออะไร → **อ่าน `STATUS.md` ก่อนทำงานเสมอ**

## 🔄 กติกาทำงานร่วมกันของทุกแชท (ชุดเดียวกับ palm-hq / origin-hq / olddays-hq)

1. **ก่อนเริ่มงานทุกครั้ง: `git pull` ก่อนเสมอ** — repo นี้มีหลายแชทแก้ได้
2. **ทำเสร็จ: commit + push ทันที** ห้ามดองงานไว้ในเครื่อง/ใน scratchpad ข้ามวัน
3. **แก้อะไรที่กระทบของจริง (GAS) → อัปเดต `STATUS.md`** (เขียนทับสถานะ ไม่ append) + **bump `CODE_VERSION`** หัวไฟล์ .gs ที่แก้
4. ประวัติการแก้ = `git log` อย่างเดียว ไม่จด changelog ซ้ำในไฟล์ .md
5. หน้าเว็บทั้งหมดเสิร์ฟจาก **GitHub Pages ของ repo นี้** → push `main` = ขึ้นจริง
   ส่วนไฟล์ `.gs` มี **deploy อัตโนมัติด้วย clasp** แล้ว (`.github/workflows/deploy-gas.yml`) — **push `main` = ขึ้น Apps Script เอง**
   เปิดใช้จริง 30 ส.ค. 2026 (URL เดิม ไม่เปลี่ยน · มี smoke test ยิง `pkHealth` เช็คว่าขึ้นจริง)
   ถ้าระบบอัตโนมัติล่ม ค่อยวางมือ: **วางจากไฟล์ใน `main` เท่านั้น** แล้ว Manage deployments → ✏️ → **New version**
   (ห้าม New deployment — URL เปลี่ยนแล้วลิงก์ที่แจกทีมพัง) → รายละเอียดทั้งหมดอยู่ **`DEPLOY.md`**
6. **แก้ `.gs` → bump `CODE_VERSION` · แก้ `.html` → bump `WEB_VERSION`** (ค่าคงที่หัวไฟล์)
   แอปโชว์สองเลขนี้ท้ายหน้า ถ้าไม่ตรงกันจะขึ้นแถบเตือนว่าหลังบ้านยังไม่ได้วาง

## 📏 ข้อตกลงข้อมูล (ทุกแชทต้องเคารพ)

- **เงินรับจริง/ลูกหนี้ของระบบกลาง อ่านจากแท็บ `Bills`** ใน `PKSystem` (ใบวางบิล = `Statements`) — **ห้ามเปลี่ยนชื่อคอลัมน์/ความหมาย** ของสองแท็บนี้ ถ้าจำเป็นให้จดใน STATUS.md แล้วบอกกันก่อน
- แชทอื่นเพิ่มแท็บใหม่ได้ (เช่น `Expenses`) แต่หาคอลัมน์จาก**หัวตาราง**เสมอ อย่าอ้างเลขคอลัมน์ตายตัว
- **ราคาสินค้า**: ประธานใหญ่ปรับผ่านบอร์ดบริหารเท่านั้น (`pkPriceSet` ล็อกด้วย `PK_EXEC_KEY`) ทุกครั้งลง `PriceHistory`
- ข้อมูลเงิน/PIN = ความลับ — ห้ามหลุดเข้ากลุ่มพนักงาน/ลูกค้า

## ⚠️ เรื่องค้างที่ต้องเคาะ (ณ 2026-08-30)

- ~~ระบบบิลมี 2 ทางซ้อนกัน~~ ✅ **เคาะแล้ว 31 ส.ค.**: ฝ่ายบัญชีอยู่ชีท "ระบบบัญชี" เดิม (`1OXqLgj4…`) ต่อไป — P&K System **ซิงก์อ่านอย่างเดียว**จากชีทนั้นทุกวัน (`syncAccounting()` · เลิก snapshot) · `finance/` พักไว้ไม่ติดตั้ง · **ห้ามเขียนกลับชีทบัญชีเด็ดขาด** — รายละเอียดใน `STATUS.md`
